resolver: lts-15.1

allow-newer: true

packages:
- '.'

extra-deps:
  - primitive-0.7.0.1
# Turn on system GHC
system-ghc: true
skip-ghc-check: true

dump-logs: all

build:
  keep-tmp-files: true
  cabal-verbose: true

extra-path:
  - /home/csaba/haskell/ghc-gitlab/bug-test/quickest/ghc/_build/stage1/bin
